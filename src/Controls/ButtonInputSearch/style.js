import styled from "styled-components";
import { Space } from "antd";
export const WrapperSearch = styled(Space.Compact)`
    width: 100%;
    position: relative;
    top: 50%;
    transform: translateY(-50%);
`;
